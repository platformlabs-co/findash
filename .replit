
modules = ["nodejs-20:v8-20230920-bd784b9", "python-3.12:v25-20230920-d4ad2e4"]
run = "uv pip install -r api/pyproject.toml && python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --app-dir api & cd dashboard && npm install && npm start"

[nix]
channel = "stable-24_05"

[deployment]
run = ["sh", "-c", "uv sync & python3 -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --app-dir api & cd dashboard && npm start"]

[[ports]]
localPort = 3000
externalPort = 80

[[ports]]
localPort = 8000
externalPort = 8000
